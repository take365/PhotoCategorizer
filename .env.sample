# ===========================
# PhotoCategorizer .env.sample
# ===========================
# このファイルを ".env" にリネームして値を埋めてください。
# 行頭の # はコメントです。値は引用符なしで入力してください。

# --- Pixabay API ---
# キーの取得方法：
# 1) Pixabay にサインアップ → ログイン
# 2) API ドキュメントの案内に従って API key を発行
#    https://pixabay.com/api/docs/
PIXABAY_API_KEY=

# --- OpenAI API ---
# キーの取得方法：
# 1) OpenAI にログイン
# 2) API Keys ページで "Create new secret key"
#    https://platform.openai.com/account/api-keys
OPENAI_API_KEY=

# --- GPT 画像判定に使うモデル（選択） ---
# * gpt-4o-mini  … 速い＆低コスト（推奨デフォルト）
# * gpt-5-mini   … 4o-mini より高精度（コストやや高）
# * gpt-5        … 最高精度（高コスト）
# ※ gpt-5-nano は画像入力非対応（使用不可）
BATCH_MODEL=gpt-4o-mini

# --- Evaluation defaults (optional) ---
# CLI 未指定時に使うデフォルトを設定できます。
# - EVAL_MODE: gpt-off / gpt-review / gptall
# - EVAL_MODEL: openclip / siglip / both
# - GPT_*: GPT 呼び出しのチューニング項目
EVAL_MODE=gpt-review
EVAL_MODEL=both
GPT_BATCH_SIZE=8
GPT_RPS=1.0
GPT_MAX_RETRIES=6
GPT_BACKOFF_BASE=0.5

# --- Optional throttling (tokens/requests per minute) ---
# 組織のレートリミットに合わせてガードしたい場合のみ設定。
# 未設定時は CLI オプション値のみで制御します。
# 例: GPT_TPM_LIMIT=180000  # 1分あたり送信トークン上限
#      GPT_RPM_LIMIT=60     # 1分あたり送信リクエスト上限
GPT_TPM_LIMIT=180000
GPT_RPM_LIMIT=60
